# 캡스톤 피드백 반영 배포 검증

구현 기준일: 2026-09-11. 최종 배포 재확인·통계 검증: 2026-09-12 (한국 시간).

## 배포 전 확인

- pytest: 61 passed (1.94s). 점수 없는 학습을 취약 개념이나 캘린더의 실패로 분류하지 않는 회귀 테스트 포함.
- 프론트 lint/build 통과.
- Python compile 및 git diff 공백 오류 검사 통과.
- 실제 설정된 OpenAI 모델로 구조화 응답 1회 호출 통과.
- 카탈로그 분포 감사: 학교급/성취기준 조합 검토 대상 43개. 확정 오류 수가 아님.
- Browser 연결 없음: 브라우저 클릭·시각 검증은 수행하지 못함.

## 배포 후 확인

### 실제 RAG·OpenAI 대화

`scripts/smoke_learning_goals.py` 실행 통과. `d1b7d96` 배포 후 실행했으며 후속 `5cb4d41`은 통계 표시만 수정한다.

- API: https://backend-production-98f3.up.railway.app
- 검증용 가상 학생, 과학 / 중학교 2학년 / `9과02`, 세션 57, 노트 7.
- Chroma 검색 결과에 `bm25_rrf` 재정렬 메타데이터 확인.
- 세 번 모두 실제 OpenAI 응답. 규칙 폴백으로 통과한 결과가 아님.
- 학습목표 유지, 힌트 횟수 증가, 대화 상태 재조회 일치, 목표 5항목 결과와 노트 저장, 종료 재요청의 결과 동일성 확인.
- 생성한 검증용 학습방만 소프트 삭제했다. 가상 계정의 종료 기록·노트는 남겼으며 사용자 기존 자료는 삭제하지 않았다.

| 입력 | 관찰 결과 | 해당 요청 시간 |
| --- | --- | ---: |
| 몰라 | 세포 개념 설명과 쉬운 선택 질문, 개념 설명 단계 유지 | 6.96초 |
| 아직 잘 모르겠어. 쉬운 예시로 설명해줘. | 세포를 레고에 비유, 직전 질문과 단계 유지 | 4.58초 |
| 조직과 기관의 정의 설명 | 내용은 인정하되 직전 질문에 답하지 않았음을 안내, `partial`, 단계 유지 | 17.53초 |

이 시간은 한 가상 대화의 요청별 관측값이며 평균 지연·동시접속 성능·학습효과 수치가 아니다.
이 대화에서는 단계 상승이 발생하지 않았다. 단계 상승 조건은 자동 테스트로 확인했으며 실제 학생의 숙달 판정 정확도를 검증한 것은 아니다.

### 최종 배포·통계 확인

최종 코드 `5cb4d4112c7b873010733f133596dedf078fb1b2`의 프론트·백엔드 모두 Railway `SUCCESS`를 확인했다.
2026-09-12 같은 커밋의 두 서비스 `SUCCESS`를 재확인했다.
이후 추가 커밋은 이 검증 기록 등 문서만 갱신하며, 실행 코드는 위 커밋과 동일하다.
준비 상태 API에서 MySQL 사용 가능, Chroma 문서 302,059개, Redis 연결, OpenAI 키 설정 여부를 확인했다.
키 설정 여부만으로 실제 호출 성공을 주장하지 않으며 실제 호출은 위의 별도 3턴 테스트에서 확인했다.

배포 전환 중 통계 검사 1회는 구버전 응답(`학습 전`)으로 실패했다. 이 결과를 최종 배포 통과로 세지 않는다.
해당 검사는 가상 계정과 빈 학습 세션·노트를 생성했고 검증 단언에서 중단돼 그 테스트 방도 남아 있다.

2026-09-12 최종 재검사 **통과**:

- 가상 학생의 새 세션 60을 종료한 뒤 대시보드에서 `미확인`, 취약 개념 목록이 비어 있음을 확인했다.
- 같은 세션의 캘린더 기록도 `미확인`으로 표시됐다. 점수 없는 기록을 실패로 분류하지 않는다.
- 프론트 도메인의 인증 헤더 포함 CORS 사전 요청 허용을 확인했다.
- 공개 프론트 URL HTTP 200과 React 진입 HTML을 확인했다. 브라우저 화면 검사와는 다르다.
- 최종 검증용 방 30은 소프트 삭제했다. 검증 계정의 완료 기록과 노트는 남겼다.
- 이전 턴에서 시작한 별도 통계 검사 프로세스는 종료 출력을 회수할 수 없어 성공 수에 포함하지 않았다.

접속: https://frontend-production-8c41.up.railway.app

## 남은 검증과 발표 시 제한

- 실제 브라우저 클릭·다크모드 시각 확인은 Browser 연결이 없어 수행하지 못했다.
- 실제 학생 대상 학습효과 및 일반 챗봇 대비 우월성은 아직 측정하지 않았다.
- 과목·학교급 메타데이터 검토 후보 43개는 원본 대조가 필요하다.
- 검색 출처 ID와 학생 인용의 존재 검사는 답변 전체의 사실 검증이 아니다.
- 선택형 CrossEncoder는 실제 모델 성능을 평가하지 않았다. 이번 운영 기본값은 BM25 + RRF다.
